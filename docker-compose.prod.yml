version: '2'
services:
  app:
    extends:
      file: docker-compose.base.yml
      service: app
    image: thomasmars/graff-node
    ports:
      - "80:8080"
    volumes:
      - appImages:/usr/appImages
    links:
      - mongo

  mongo:
    image: mongo
    volumes:
      - mongodata:/data/db

volumes:
  mongodata:
  appImages:
